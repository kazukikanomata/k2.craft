## コンセプト

ITエンジニアが学んだこと・調べたことを発信する技術ブログ。
白ベースのグレースケールに、アクセントカラーを1色だけ効かせるミニマルデザイン。
2025年のWebデザイン潮流（モノクロ＋差し色1色、大胆なタイポグラフィ、静けさのある余白）を踏まえつつ、
Stripeドキュメントのようなグレースケール＋モノスペースフォントの技術的な質感を狙う。
幾何学モチーフ（正方形・キューブ）をサイト全体のコンポーネントに一貫して適用する。

## カラーパレット

無彩色をベースに、アクセントカラーは以下の1色のみを使う。装飾目的での色追加はしない。

| 用途 | 値 | 備考 |
| --- | --- | --- |
| 背景 | `#FFFFFF` | 白ベース |
| テキスト（本文） | `#171717` | 純黒ではなく柔らかい濃グレー |
| テキスト（メタ情報・補足） | `#6B7280` | 日付・キャプションなど |
| ボーダー（通常） | `#E5E7EB` | カード・区切り線 |
| ボーダー（強調・ホバー） | `#9CA3AF` | ホバー時の枠強調 |
| アクセント | `#2563EB` | リンク・キューブ演出・タグ・h2ボーダーなど、操作可能要素にのみ使用 |
| アクセント（ホバー） | `#1D4ED8` | アクセント要素のホバー状態 |

## タイポグラフィ

- 本文・見出し: `Noto Sans JP`（既存踏襲、可変ウェイト）
- 日付・タグ・コードブロックなどのメタ情報: `JetBrains Mono`（モノスペース）
  - 技術ブログらしい「手触り感」を出すため、本文と明確に書体を切り替える
- ヒーローのサイトタイトルのみ: `DotGothic16`（ドットマトリックス調のディスプレイ書体）
  - キューブ演出のドット表現とトーンを揃えるための限定的な例外使用。文字間を広め（`tracking 0.18em`程度）に取り、ドット感を強調する
- 見出しは装飾を増やさず、ウェイトとサイズの差で階層をつくる（大胆なタイポグラフィ＝太さとサイズで魅せる方針）

## 幾何学モチーフ（サイト全体で統一）

正方形・キューブを基本図形とし、角丸を使わない直線的な世界観をサイト全体に適用する。

- **カード（記事一覧サムネイル等）**: 角丸なし、`1px solid #E5E7EB`。ホバーでボーダーを`#2563EB`に変化（背景・影は変えない）
- **カテゴリータグ・バッジ**: 角丸なしの四角形。通常はボーダーのみ、ホバー/選択中はアクセント色で塗りつぶし
- **h2見出し**: 左に太いボーダー（`4px solid #2563EB`）＋左パディングで区切りを表現
- **区切り線**: 小さな正方形ドットを連ねた装飾線、または`1px`の直線
- **ページネーション**: 角丸なしの四角ボタン。現在ページはアクセント色で塗りつぶし
- **目次（TOC）ボックス**: 角丸なしのボーダーボックスに統一
- **パンくずリスト**: `JetBrains Mono`・`text-xs`、項目間は`/`区切り。リンク項目はホバーでアクセントカラー、現在地（末尾項目）はリンクにせず本文色（`#171717`）で表示

## ヘッダー

- サイトロゴ（正方形アクセントカラーのドット）＋サイトタイトルを表示するシンプルな1行構成
- ナビゲーションリンク（About等）はヘッダーには設置しない。固定ページへの動線は各ページのパンくずリストに委ねる
- ヒーロー演出があるページ（トップページ）ではヘッダー自体を表示しない（ヒーロー内で大きくサイトタイトルを表示するため、重複を避ける）

## パンくずリスト

- トップページ（`/`、`/p/[page]`のうちページ1）を除く全ページに設置: About、記事詳細、カテゴリー一覧、各ページネーションページ
- 表示位置: ヘッダー直下・メインコンテンツ（2カラムグリッド）の直前、全幅の帯として表示
- 形式: `Home / カテゴリー名 / 記事タイトル` のように現在地までのパスを`/`区切りで表示（先頭は常に`Home`固定）
  - 記事詳細ページ: `Home / カテゴリー / 記事タイトル`（カテゴリー未設定時は`Home / 記事タイトル`）
  - カテゴリー一覧ページ: `Home / カテゴリー名`
  - ページネーションページ: `Home / Page N`（カテゴリー配下の場合は`Home / カテゴリー名 / Page N`）

## トップページの構成・演出

### レイアウト
- 上部: サイトタイトル＋ヒーロー演出エリア
- 下部: 2カラム構成を継続
  - 左（約3fr）: ブログ記事一覧（カードグリッド、サムネイル＋タイトル＋日付[mono]＋抜粋＋カテゴリータグ）
  - 右（約1fr）: カテゴリー一覧サイドバー（常設）

### ヒーロー演出仕様
- 3x3グリッドの溝（グルーブ）を持つ立方体（ルービックキューブ状のシルエット）を、多数の小さな正方形（ドット）で構成する、ドットマトリックス（halftone/dither）調のビジュアル
- 実装は`<canvas>` + 自前の3D回転投影（立方体6面それぞれをグリッド状の点群として生成し、各面内の境界線（±1/3の位置）付近の点を間引くことで溝を表現）。Three.js等の3Dライブラリは導入せず、外部依存なしで完結させる
- 光源方向（左上手前）と各面の法線のなす角から面ごとの濃淡（陰影）を計算し、正方形の密度・不透明度・サイズにディザとして反映する。3x3の各マス（ステッカー）ごとにも明度をわずかに変化させ、面の分割を感じさせる
- 各点の法線を回転に合わせて変換し、カメラを向いていない裏面は描画しない（裏面カリング）。常に手前2〜3面のみが見える、実在感のあるキューブとして描画する
- Y軸回転に加え、緩やかなサイン波によるチルト（X軸方向の揺れ）を加えることで、単調にならない回転にする
- ページ読み込み後、ゆっくりと連続的に回転し続ける（一度きりの演出ではなく常時アニメーション）
- 色はアクセントカラー（`#2563EB`）の不透明度違いのみを使用し、背景は白のまま
- `prefers-reduced-motion: reduce`環境では回転を止め、固定角度の静止フレームを1枚描画する
- サイズは`clamp(220px, 46vw, 320px)`とし、モバイルでは自動的に縮小する

## 記事詳細ページの構成

- ページ最上部にサムネイル画像をフル幅で表示（角丸なし）
- タイトル（h1）
- 日付（monoフォント）＋カテゴリータグ（角丸なし四角バッジ）
- 目次（TOCボックス、角丸なし）
- 本文: 可読性を最優先したシンプルなWordPress記事的レイアウト。行間広め、h2は左ボーダースタイルを適用
- コードブロック: モノスペースフォント、グレースケール配色（現行の配色踏襲でよい）

## レスポンシブ・アクセシビリティ

- モバイルではサイドバー（カテゴリー一覧）は記事一覧の下に回り込む
- ヒーロー演出は`prefers-reduced-motion`に対応必須
- アクセントカラー以外に情報を伝える手段（下線・太さ・アイコン等）を必ず併用し、色だけに依存しない

## 参考ページ

https://coliss.com/
https://www.webcreatorbox.com/
