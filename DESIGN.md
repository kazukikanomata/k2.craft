## コンセプト

ITエンジニアが学んだこと・調べたことを発信する技術ブログ。
白ベースのグレースケールに、アクセントカラーを1色だけ効かせるミニマルデザイン。
2025年のWebデザイン潮流（モノクロ＋差し色1色、大胆なタイポグラフィ、静けさのある余白）を踏まえつつ、
Stripeドキュメントのようなグレースケール＋モノスペースフォントの技術的な質感を狙う。
幾何学モチーフ（正方形・キューブ）をサイト全体のコンポーネントに一貫して適用する。

## 設計原則(なぜこのルールがあるか)

以降のセクションは個別の値・クラス名を定めているが、その背後にある考え方は次の4つに集約される。新しいコンポーネントを作る際、ここに当てはまるかで判断する。

1. **抑制されたカラー** — 色は情報を伝えるための最小限の手段。装飾目的で色数を増やさない。アクセントは背景塗りではなく線・テキストとして使う(唯一の例外はPrimaryボタン)
2. **無彩色は一貫したトークンで管理する** — 「カラーパレット」節で定義したトークン(背景・テキスト・ボーダー)以外の色みを持つグレーを混在させない。同じ役割の要素が場所によって微妙に違うグレーになる状態を避ける
3. **鋭利なジオメトリー** — 角丸を多用しない。正方形・直線を基本図形とし、pill形状は使わない
4. **可読性を装飾より優先する** — 日本語の文章を読ませることが目的のサイトなので、余白・行間・組版の細部を、見た目のインパクトより優先する

## カラーパレット

無彩色をベースに、アクセントカラーは以下の1色のみを使う。装飾目的での色追加はしない。

| 用途 | 値 | 備考 |
| --- | --- | --- |
| 背景 | `#FFFFFF` | 白ベース |
| テキスト（本文） | `#171717` | 純黒ではなく柔らかい濃グレー |
| テキスト（メタ情報・補足） | `#6B7280` | 日付・キャプションなど |
| ボーダー（通常） | `#E5E7EB` | カード・区切り線 |
| ボーダー（強調・ホバー） | `#9CA3AF` | ホバー時の枠強調 |
| アクセント | `#2563EB` | リンク・キューブ演出・タグ・h2ボーダー・h3下線など、操作可能要素にのみ使用 |
| アクセント（ホバー） | `#1D4ED8` | アクセント要素のホバー状態 |

### カラーパレットの制約

無彩色は上表のトークン(`ink`/`muted`/`line`/`line-strong`)を優先して使う。トークンでカバーできない濃淡が必要な場合のみ、Tailwindの`gray`パレットを使ってよい。`slate`/`zinc`/`neutral`/`stone`など、同じ役割を持つ他のグレースケールパレットは混在させない(同じ「ボーダーの薄いグレー」でもファイルによって微妙に色相が違う、という状態を防ぐため)。

彩度のあるパレットは、下記のアラート専用の4色(`blue`/`green`/`yellow`/`red`)を除いて使用しない。新しく必要になった場合は、使うパレットと用途をこの節に追記してから使用する。

**例外(記事本文のMarkdown表現):**

- シンタックスハイライト: コードブロックはshikiのテーマ(`github-light` / `github-dark`)が持つ配色をそのまま使う。トークンの種類を色で見分けるという機能上の理由による例外で、ブロックの背景・枠線は無彩色トークンのまま
- 記事アイコン: 記事ごとの絵文字(`icon`)は、OS標準の絵文字フォントの配色で表示する。サムネイル画像の代わりに記事上部と一覧カードに置く。背景の枠は無彩色トークンのまま
- アラート(`> [!WARNING]`など): 種類ごとに背景(薄い`50`番台)・左の太いボーダー・タイトルの色を変える。色だけに依存しないよう、タイトル(アイコン+種類名)を必ず併用する。記事本文のスタイル(`app/src/styles/global.css`の`.markdown-alert-*`)でのみ使い、コンポーネントでは使わない

  | 種類 | パレット | 背景 | ボーダー・タイトル(ライト) | ボーダー・タイトル(ダーク) |
  | --- | --- | --- | --- | --- |
  | `NOTE` / `IMPORTANT` | `blue` | `blue-50`(ダーク: `blue-950`) | `blue-600` | `blue-400` |
  | `TIP` | `green` | `green-50`(ダーク: `green-900`) | `green-700` | `green-400` |
  | `WARNING` | `yellow` | `yellow-50`(ダーク: `yellow-900`) | `yellow-700` | `yellow-500` |
  | `CAUTION` | `red` | `red-50`(ダーク: `red-950`) | `red-700` | `red-400` |

**許可されている無彩色パレット:** `gray`(トークン非対応の濃淡のみ)

**使用しないパレット:** `slate`, `zinc`, `neutral`, `stone`, および用途未定義の彩度パレット全般(アラート専用の`blue`/`green`/`yellow`/`red`以外)

この制約は`app/src/styles/global.css`の`@theme`で無効化し(アラート専用の4色を除く)、ESLintでも動的クラス名を検出する(アラート専用の4色も、`.astro`/`.ts`での使用はESLintで禁止する)(「制約の実施方針」参照)。

## ダークモード

初回訪問時はOSの`prefers-color-scheme`に従い、ヘッダー/ヒーローのボタンで明示的に切り替えると`localStorage`に記憶する(以降はOS設定より優先)。

| 用途 | ライト | ダーク |
| --- | --- | --- |
| 背景 | `#FFFFFF` | `#111111` |
| テキスト（本文） | `#171717` | `#F2F2F2` |
| テキスト（メタ情報） | `#6B7280` | `#A3A3A3` |
| ボーダー（通常） | `#E5E7EB` | `#333333` |
| ボーダー（強調） | `#9CA3AF` | `#545454` |
| アクセント | `#2563EB`（両モード共通） | 同左 |

### 設計判断

- **アクセントカラーは変えない**: 背景が`#111111`程度まで暗ければ`#2563EB`のままでも十分なコントラストが取れるため、ダーク用の変数を追加しない(「抑制されたカラー」原則: 変数を増やさない)
- **トークンの上書きで実現する**: `bg`/`ink`/`muted`/`line`/`line-strong`をカラートークンとして扱い、`.dark`セレクタ配下で同じCSS変数を上書きする。個々のコンポーネントに`dark:`バリアントを書かせるのではなく、既存の`bg-*`/`text-ink`/`border-line`などのクラスがそのまま両モードに追従する設計にする(実装: `app/src/styles/global.css`の`@variant dark`)
- **トークンでカバーできない`gray-*`は個別に`dark:`を追加する**: 見出しの階層(h1〜h5)やテーブル・コードブロックなど、意図的に濃淡を使い分けている箇所はトークン化せず、既存の階調を保ったまま`dark:`バリアントを追加する。アラートの色も同様に、種類ごとに`dark:`バリアントで明るい色へ切り替える
- **FOUC対策**: `<head>`内の同期スクリプトで、CSS読み込み・描画前に`localStorage`の値(またはOS設定)から`.dark`クラスを確定させる。OS設定変更のリアルタイム反映は、明示的な選択(`localStorage.theme`)が無い場合のみ行う

## タイポグラフィ

- 本文・見出し: `Noto Sans JP`（既存踏襲、可変ウェイト）
- 日付・タグ・コードブロックなどのメタ情報: `JetBrains Mono`（モノスペース）
  - 技術ブログらしい「手触り感」を出すため、本文と明確に書体を切り替える
- ヒーローのサイトタイトルのみ: `DotGothic16`（ドットマトリックス調のディスプレイ書体）
  - キューブ演出のドット表現とトーンを揃えるための限定的な例外使用。文字間を広め（`tracking 0.18em`程度）に取り、ドット感を強調する
- 見出しは装飾を増やさず、ウェイトとサイズの差で階層をつくる（大胆なタイポグラフィ＝太さとサイズで魅せる方針）

### 日本語組版の細部

技術ブログとして日本語の文章を長時間読ませるため、以下をグローバル(`:root`)または記事本文(`.entry-content`)に適用する。

| CSSプロパティ | 値 | 適用先 | 効果 |
| --- | --- | --- | --- |
| `overflow-wrap` | `anywhere` | `:root` | 収まらない語を折り返す |
| `line-break` | `strict` | `:root` | 禁則処理を厳格に適用する |
| `text-autospace` | `normal` | `:root`（`pre`/`time`/`input`/`textarea`/`[contenteditable]`は`no-autospace`） | 日本語と英数字の間にスペースを自動挿入する |
| `text-spacing-trim` | `trim-start`（`pre`は`space-all`） | `:root` | 行頭の約物スペースを除去する |
| `font-kerning` | 本文`none` / 見出し`normal` | `:lang(ja)` | 本文はベタ組みを維持し、見出しのみカーニングを効かせる |
| `font-feature-settings: "palt"` | 見出しのみ | `:lang(ja)`の見出し | 見出しの約物をプロポーショナル字詰めにする。**本文には適用しない**(段落中に不自然な空行が生まれるため) |
| `word-break: auto-phrase` + `text-wrap: balance` | 見出しのみ | 対応ブラウザの見出し | 文節単位の改行と行長バランス調整。**本文には適用しない**(可読性が悪化するため) |

設計判断: 本文はベタ組みのまま、装飾的な字詰め・改行制御は見出しだけに限定する。本文に手を加えると1行あたりの情報量が不安定になり、読書のリズムを崩すため。

## 幾何学モチーフ（サイト全体で統一）

正方形・キューブを基本図形とし、角丸を使わない直線的な世界観をサイト全体に適用する。

- **カード（記事一覧等）**: 角丸なし、`1px solid #E5E7EB`。ホバーでボーダーを`#2563EB`に変化（背景・影は変えない）
- **カテゴリータグ・バッジ**: 角丸なしの四角形。通常はボーダーのみ、ホバー/選択中はアクセント色で塗りつぶし
- **h2見出し**: 左に太いボーダー（`4px solid #2563EB`）＋左パディングで区切りを表現
- **h3見出し**: 全幅の薄いボーダー（`3px solid #E5E7EB`）の上に、文字幅だけアクセント色（`#2563EB`）の下線を重ねる。文字部分は`rehypeHeadingSpan`が付与する`<span>`で囲む。アンカー（#）は`<span>`の外に置くため下線の幅に含まれない
- **区切り線**: 小さな正方形ドットを連ねた装飾線、または`1px`の直線
- **ページネーション**: 角丸なしの四角ボタン。現在ページはアクセント色で塗りつぶし
- **目次（TOC）ボックス**: 角丸なしのボーダーボックスに統一
- **パンくずリスト**: `JetBrains Mono`・`text-xs`、項目間は`/`区切り。リンク項目はホバーでアクセントカラー、現在地（末尾項目）はリンクにせず本文色（`#171717`）で表示

## ヘッダー

- サイトロゴ（正方形アクセントカラーのドット）＋サイトタイトルを表示するシンプルな1行構成
- ナビゲーションリンク（About等）はヘッダーには設置しない。固定ページへの動線は各ページのパンくずリストに委ねる
- ヒーロー演出があるページ（トップページ）ではヘッダー自体を表示しない（ヒーロー内で大きくサイトタイトルを表示するため、重複を避ける）

## パンくずリスト

- トップページ（`/`、`/p/[page]`のうちページ1）と記事詳細ページを除く全ページに設置: About、カテゴリー一覧、各ページネーションページ（記事詳細ページは左上の「Back to blog」リンクで代える）
- 表示位置: ヘッダー直下・メインコンテンツ（2カラムグリッド）の直前、全幅の帯として表示
- 形式: `Home / カテゴリー名 / 記事タイトル` のように現在地までのパスを`/`区切りで表示（先頭は常に`Home`固定）
  - カテゴリー一覧ページ: `Home / カテゴリー名`
  - ページネーションページ: `Home / Page N`（カテゴリー配下の場合は`Home / カテゴリー名 / Page N`）

## トップページの構成・演出

### レイアウト
- 上部: サイトタイトル＋ヒーロー演出エリア
- 下部: 2カラム構成を継続
  - 左（約3fr）: ブログ記事一覧（カードグリッド、絵文字アイコン＋タイトル＋日付[mono]＋抜粋）
  - 右（約1fr）: カテゴリー一覧サイドバー（常設）

### ヒーロー演出仕様
- 3x3グリッドの溝（グルーブ）を持つ立方体（ルービックキューブ状のシルエット）を、多数の小さな正方形（ドット）で構成する、ドットマトリックス（halftone/dither）調のビジュアル
- 実装は`<canvas>` + 自前の3D回転投影（立方体6面それぞれをグリッド状の点群として生成し、各面内の境界線（±1/3の位置）付近の点を間引くことで溝を表現）。Three.js等の3Dライブラリは導入せず、外部依存なしで完結させる
- 光源方向（左上手前）と各面の法線のなす角から面ごとの濃淡（陰影）を計算し、正方形の密度・不透明度・サイズにディザとして反映する。3x3の各マス（ステッカー）ごとにも明度をわずかに変化させ、面の分割を感じさせる
- 各点の法線を回転に合わせて変換し、カメラを向いていない裏面は描画しない（裏面カリング）。常に手前2〜3面のみが見える、実在感のあるキューブとして描画する
- Y軸回転に加え、緩やかなサイン波によるチルト（X軸方向の揺れ）を加えることで、単調にならない回転にする
- ページ読み込み後、ゆっくりと連続的に回転し続ける（一度きりの演出ではなく常時アニメーション）
- 色はアクセントカラー（`#2563EB`）の不透明度違いのみを使用し、背景は白のまま
- `prefers-reduced-motion: reduce`環境では回転を止め、固定角度の静止フレームを1枚描画する
- サイズは`clamp(220px, 46vw, 320px)`とし、モバイルでは自動的に縮小する

## 記事詳細ページの構成

Layoutのメイン2カラムグリッドは使わず、記事ページだけ全幅(`md:col-span-2`)で独自に組む。上から順に:

1. 左上に「Back to blog」リンク(`max-w-6xl`の枠の左端。`/`へ戻る)
2. 記事ヘッダー(`max-w-3xl`の狭い列を画面中央に置く。文字は左揃え)
   - 絵文字アイコン(列の中央に大きく表示。サムネイル画像は使わない)
   - タイトル(h1)
   - 日付(カレンダーアイコン＋monoフォント)
   - カテゴリータグ(`#カテゴリー名`。角丸なし四角バッジ)と、右端に「Markdownをコピー」ボタン
   - 記事の概要(`description`)
   - 目次(1024px未満のみ。角丸なしのボーダーボックス)
3. 本文と目次の2カラム(`max-w-[1100px]`を画面中央に置き、本文756px + 目次240pxの間に2.5rem。1024px以上)
   - 本文: 可読性を最優先したシンプルなレイアウト。行間広め、h2は左ボーダースタイルを適用
   - 目次: 本文の右側にスクロール追従(`sticky`)で表示。1024px未満では記事ヘッダー内に移る
   - コードブロック: モノスペースフォント、グレースケール配色(現行の配色踏襲でよい)
4. 共有ボタン(`max-w-3xl`、上に区切り線)
5. 関連記事(`max-w-6xl`)

## レスポンシブ・アクセシビリティ

- モバイルではサイドバー（カテゴリー一覧）は記事一覧の下に回り込む（記事詳細ページにサイドバーはなく、目次は1024px未満で記事ヘッダー内に移る）
- ヒーロー演出は`prefers-reduced-motion`に対応必須
- アクセントカラー以外に情報を伝える手段（下線・太さ・アイコン等）を必ず併用し、色だけに依存しない

## 制約の実施方針

DESIGN.mdはあくまで**設計意図の記録**であり、制約の実施はツールで行う。ドキュメントに具体的なクラス名を列挙して「禁止」と書くだけでは、書く側がドキュメントを見ずにコードを書くため効果が薄い。

### 実施済みの制約

| 制約 | 実施手段 |
| --- | --- |
| 未使用カラーパレットの無効化 | `app/src/styles/global.css`の`@theme`で対象パレットを`initial`に設定(アラート専用の`blue`/`green`/`yellow`/`red`を除く) + ESLintの`no-restricted-syntax`ルールで動的クラス名(文字列結合等)も検出(アラート専用の4色も含め、`.astro`/`.ts`での使用を禁止) |

### コードレビューで確認すべき観点(ツールで機械的に弾けないもの)

- 新しく追加したUIが「設計原則」の4項目のいずれかに沿っているか
- 見出し以外に`palt`や`auto-phrase`が適用されていないか
- 彩度のあるパレットを新しく使う場合、「カラーパレットの制約」節に用途を追記してから使っているか

## 参考ページ

https://coliss.com/
https://www.webcreatorbox.com/
